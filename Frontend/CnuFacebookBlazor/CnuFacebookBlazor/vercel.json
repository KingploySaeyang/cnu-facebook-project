{
  "buildCommand": "dotnet publish -c Release -o release",
  "outputDirectory": "release/wwwroot",
  "cleanUrls": true,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
